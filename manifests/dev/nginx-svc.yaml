apiVersion: v1
kind: Service
metadata:
  name: engineering-server-service
spec:
  selector:
    app: engineering-server
  ports:
    - protocol: TCP
      port: 80         # Port exposed by the service
      targetPort: 8080 # Port the container is listening on
  type: ClusterIP      # Use NodePort or LoadBalancer if external access is needed