apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-configmap
data:
  # property-like keys; each key maps to a simple value
  database_url: "https://rds.ap-south-1a.amazon.com"
  database_username: "admin"
